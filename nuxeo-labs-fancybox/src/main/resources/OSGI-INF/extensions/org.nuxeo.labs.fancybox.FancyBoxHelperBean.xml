<?xml version="1.0"?>

<component name="org.nuxeo.labs.fancybox.FancyBoxHelperBean">
<!-- This is the default extension generated by Nuxeo Wizard.

     We don't need it because we declare 1-n custom contributions, each of them
     using the Fancybox bean.
     We leave it here for no special reason actually :-), but commented, because
     if we don't comment it, it will add a button to about all SUBVIEW_UPPER_LIST,
     or generate an error because the accept() method is not implemented in the
     bean (but referenced in the filter)
     
     See the sample to get an example of contribution to use with this specific plug-in
-->
     
  <!--
  <extension target="org.nuxeo.ecm.platform.actions.ActionService"
    point="actions">

    <action id="org.nuxeo.labs.fancybox.FancyBoxHelperBean" link="#{fancyBoxHelper.doGet}"
      order="200" label="label.org.nuxeo.labs.fancybox.FancyBoxHelperBean"
      icon="/icons/org.nuxeo.labs.fancybox.FancyBoxHelperBean-action.png">
      <category>SUBVIEW_UPPER_LIST</category>
      <filter-id>org.nuxeo.labs.fancybox.FancyBoxHelperBean</filter-id>
    </action>
  </extension>

  <extension target="org.nuxeo.ecm.platform.actions.ActionService"
    point="filters">

    <filter id="org.nuxeo.labs.fancybox.FancyBoxHelperBean">
      <rule grant="true">
        <condition>#{fancyBoxHelper.accept()}</condition>
      </rule>
    </filter>

  </extension>
  -->
</component>
